module.exports = {
 imagemin: {
  options: {
   title: 'Build complete',
   message: '<%= pkg.name %> build finished successfully.'
  }
 },
 compass: {
  options: {
   title: 'Build complete',
   message: '<%= pkg.name %> build finished successfully.'
  }
 },
 concat: {
  options: {
   title: 'Build complete',
   message: '<%= pkg.name %> build finished successfully.'
  }
 },
 uglify: {
  options: {
   title: 'Build complete',
   message: '<%= pkg.name %> build finished successfully.'
  }
 },
 watch: {
  options: {
   title: 'Build complete',
   message: '<%= pkg.name %> build finished successfully.'
  }
 }
}